# Este archivo sirve como ejemplo. Copia este contenido a un archivo .env
# y rellena los valores correspondientes para tu entorno de desarrollo.

# URL de la base de datos (usando SQLite por defecto)
# Para PostgreSQL, sería algo como: "postgresql://USER:PASSWORD@HOST:PORT/DATABASE"
DATABASE_URL="file:./dev.db"

# Configuración de NextAuth.js
NEXTAUTH_URL="http://localhost:3000"
# Genera una clave secreta segura con: openssl rand -base64 32
NEXTAUTH_SECRET=""

# Credenciales opcionales para el script de seed (db:seed)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="password123"
